# Host-side dependencies only.
#
# On-target dependencies (micro-ROS, the CEVA and u-blox vendor drivers) come
# from the ESP-IDF component manager instead — see main/idf_component.yml.
# Conan does not have a working xtensa-esp32s3 cross story, and fighting
# ESP-IDF's own component system to get one would buy nothing.

[requires]
gtest/1.15.0

[generators]
CMakeDeps
CMakeToolchain

[layout]
cmake_layout
